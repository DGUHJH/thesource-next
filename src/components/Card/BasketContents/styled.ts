import { Box } from '@material-ui/core';
import styled from 'styled-components';

export const Root = styled(Box)`
  width: 200px;
  height: 128px;
  border-radius: 5px;
  overflow: hidden;
  position: relative;
  cursor: pointer;
`;
